# 📋 AG-Análisis de Validación y Requerimientos (Vídeos Andy) - AS Operadora

**Fecha:** 18 de Agosto de 2026 - 22:25 CST  
**Versión de partida:** v2.486  
**Rama:** `dev`  
**Autor:** AntiGravity AI Assistant  
**Fuentes analizadas:** `docs/REV-CAMBIOS11808/ConversaciónValidación.docx` y 16 vídeos de pantalla/audio (`.mp4`) en `docs/REV-CAMBIOS11808/`.

---

## 🌟 Resumen Ejecutivo

Este documento consolida la transcripción exhaustiva, el análisis funcional y la matriz de requerimientos solicitados por Andy y Sergio para el **Sitio Web** y la **Aplicación Móvil PWA**. Sirve como documento base de contraste (*benchmark*) para auditar y verificar las implementaciones posteriores.

---

# 1. 🌐 Sitio Web (Portal Web & Dashboards Administrativos)
*Fuentes: `SisActividades.mp4`, `SisViajesGrupales.mp4`, `SisViajesGrupales2.mp4`, `ConversaciónValidación.docx`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Identidad Visual & Formatos (PDFs y Correos)** | Andy y Sergio acordaron que los correos institucionales, comprobantes de pago (recibos) en PDF, itinerarios en PDF y mensajes deben cambiar el color azul por **negro institucional**. En la app, al querer descargar el PDF del itinerario se descargaba un documento en blanco; debe descargar el PDF oficial completo tal como en la web. | • Generador de PDFs y templates HTML de correos creados.<br>• Logo oficial de AS Operadora integrado. | **En palabras simples:** Cambiar todas las partes azules de los correos y PDFs para que ahora sean negras elegantes, y arreglar la descarga del itinerario para que ya no salga la hoja en blanco sino el viaje con todos sus datos.<br><br>• *Técnico:* Modificar variables de acento en `src/services/email/` y utilidades de PDF para sustituir `#0066FF`/`#2563EB` por `#111827`/`#000000`. Conectar la ruta `/api/itineraries/[id]/pdf` con datos reales para evitar buffers vacíos en Next.js. |
| **Buscador de Viajes Grupales (`/tours`)** | En la página web de viajes grupales, Andy pide **quitar el video de YouTube de MegaTravel de fondo** y reemplazarlo por un **carrusel de fotos de destinos** usando las APIs de imágenes. Mencionó que buscaron el tour de Europa y no aparecía, y que en otros viajes (ej. Tailandia) la información de visado se repetía hasta 5 veces. Además, al dar clic en "Atrás" el sistema desloguea al usuario aunque haya marcado "Recuérdame". | • Conexión base y sincronización con MegaTravel funcional.<br>• Buscador por filtros y cotizador grupal operativo. | **En palabras simples:** Quitar el video de YouTube que sale al principio y poner fotos bonitas rotando de los destinos; arreglar que no se repitan los textos de visas muchas veces, asegurar que aparezca el tour de Europa y evitar que al dar atrás se cierre la sesión.<br><br>• *Técnico:* En `src/components/SearchModule.tsx` y `ToursSearch.tsx`, remover el `<iframe>` de YouTube y montar un `Carousel` con Unsplash/Cloudinary. En `MegaTravelSyncService.ts`, desduplicar arrays de `visa_requirements` y ajustar `AuthContext` para que el token en `localStorage`/`cookies` persista al usar el botón atrás del navegador (`popstate`). |
| **Buscador de Actividades & Proveedores** | Andy menciona que actualmente la web está conectada a Civitatis, pero que ya tienen convenio y credenciales con **Viator** (con enlace de afiliado para comisiones), **Big Bus** y **GetYourGuide**, y pide integrarlos todos juntos para que el usuario elija entre todas las opciones. Pide además conectar los entornos de prueba (sandbox) de Hoteles y Vuelos para que la compra emita la documentación a la app. | • Agregador `ActivityAggregator` con adaptador de Civitatis y estructura de Viator creados en backend. | **En palabras simples:** Conectar las cuentas de Viator, Big Bus y GetYourGuide con las claves que mandará Andy para que salgan todas las actividades y tours de esos proveedores en un solo buscador con sus enlaces de comisión.<br><br>• *Técnico:* Activar `ViatorAdapter`, `GetYourGuideAdapter` y `BigBusAdapter` en `src/services/travel/activities/`, inyectando las API keys y `affiliate_id` en `.env.local` para deduplicar y retornar resultados combinados en `GET /api/activities/search`. |
| **Panel de Empresas (`/dashboard/corporate`)** | Andy solicita agregar un apartado de **Agenda / Calendario** para visualizar de forma rápida e intuitiva los viajes que tienen programados las empresas, así como habilitar el resto de opciones de **Ajustes de Políticas Corporativas** (no solo hoteles y vuelos). | • Pestañas activas: Resumen, Empleados, Gastos, Métricas & CO2, Aprobaciones, Políticas, Métodos de Pago. | **En palabras simples:** Poner una pestaña de Calendario para que las empresas vean en qué días viajan sus empleados y permitirles configurar todas las reglas de gastos de la empresa.<br><br>• *Técnico:* Añadir la pestaña `agenda` / `calendario` en `src/app/dashboard/corporate/page.tsx` con un componente de calendario interactivo mensual/semanal alimentado por `GET /api/corporate/bookings/calendar`. Extender la matriz de políticas a viáticos y autorizaciones multinivel. |
| **Panel de Agencias (`/dashboard/agency`)** | Andy notó que el apartado de **Comisiones** y el de **Ventas** podían agruparse dentro de un mismo panel consolidado para que la agencia no tenga que estar saltando entre pantallas. | • Cálculo de split de comisiones (agencia vs agente) y tabla de dispersiones operativas. | **En palabras simples:** Juntar las pantallas de ventas y comisiones en una sola para que la agencia vea cuánto vendió y cuánto le toca de comisión en el mismo lugar.<br><br>• *Técnico:* Fusionar la vista de `commissions` y métricas de facturación de agentes en `src/app/dashboard/agency/page.tsx`, desplegando gráficos de volumen de venta junto con la tabla de comisiones por estado (`pending`, `available`, `paid`). |
| **Datos Bancarios en Pagos** | Andy solicitó que en los ajustes de pagos se puedan registrar y configurar los **datos bancarios (Banco, Titular, CLABE/IBAN, Referencia)** para poder compartirlos fácilmente con los clientes y permitir pagos por transferencia/ventanilla. | • Pasarelas de tarjeta (Stripe, PayPal, MercadoPago) activas en `/checkout`. | **En palabras simples:** Crear un espacio donde la empresa ponga sus números de cuenta de banco y CLABE para que los clientes puedan transferirles y subir su comprobante.<br><br>• *Técnico:* Crear tabla/campos `agency_bank_accounts` en Neon DB y endpoints `GET/PUT /api/agency/bank-details`, habilitando la visualización de datos bancarios y botón de "Copiar CLABE" en la vista de cobros. |

---

# 2. 📱 Aplicación Móvil (PWA / `/mobile`)

---

### A. 👤 Perfil (`/mobile/perfil`)
*Fuentes: `perfil.mp4`, `perfil2.mp4`, `SisViajesGrupales.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Agrupación de Datos Personales** | Andy pide que en lugar de que Nombre, Fecha de nacimiento, Correo y Teléfono aparezcan como filas/botones separados con flechas individuales, **se junten todos en una sola tarjeta/módulo unificado** con una sola flecha de acceso que lleve a la pantalla de edición. | • Vista de perfil con avatar, logo y datos del usuario visibles. | **En palabras simples:** Meter el nombre, fecha de cumpleaños, correo y teléfono dentro de una sola cajita con una única flecha para editar todo junto.<br><br>• *Técnico:* En `src/app/mobile/perfil/page.tsx`, reemplazar las 4 filas separadas por un único contenedor `<div className="rounded-2xl bg-white p-4 border ...">` que liste los 4 campos y tenga un solo `onClick={() => router.push('/mobile/perfil/editar')}`. |
| **Campo Correo en Editar Perfil** | Andy remarcó específicamente que en la pantalla de "Editar perfil" (`/mobile/perfil/editar`) **hace falta el campo de Correo electrónico**, ya que actualmente solo aparecen Nombre, Fecha de nacimiento y Teléfono. | • Formulario de edición con inputs para Nombre, Fecha y Teléfono y botón "Guardar Cambios". | **En palabras simples:** Agregar la cajita para ver y escribir el correo en la pantalla de editar perfil porque ahorita no aparece.<br><br>• *Técnico:* Añadir el `<Input type="email" value={formData.email} onChange={...} />` con su respectivo `<Label>Correo Electrónico</Label>` en `src/app/mobile/perfil/editar/page.tsx` y enviarlo en el `PUT /api/mobile/profile`. |
| **Autocompletado de Datos (Web $\rightarrow$ App)** | Andy indica que todos los datos solicitados (nombre, fecha de nacimiento, correo, teléfono) deben jalarse automáticamente de la base de datos desde que el usuario se registra en la web para evitar que tenga que llenar datos dos veces. | • Endpoint `/api/mobile/profile` lee del token JWT y tabla `users`. | **En palabras simples:** Que la app llene solita los datos del usuario tomándolos de su cuenta creada en la página web.<br><br>• *Técnico:* Asegurar que `GET /api/mobile/profile` consulte las columnas `first_name`, `last_name`, `birth_date`, `email`, `phone` en PostgreSQL y prellene el estado de React en `useEffect`. |
| **Reordenamiento: Contactos de Emergencia** | Andy pide colocar el módulo de **Contactos de Emergencia justo arriba del Seguro de Viajero** para que queden todos los datos personales agrupados. Debe ser una tarjeta separada que al tocarla abra directo la sección de agregar/editar contactos. | • Modal y lista para agregar (+ Agregar), editar parentesco, teléfono y borrar 🗑️ contactos. | **En palabras simples:** Mover la sección de contactos de emergencia para que quede arriba de la del seguro de viajero.<br><br>• *Técnico:* En `src/app/mobile/perfil/page.tsx`, reordenar el bloque JSX de `Contactos de Emergencia` colocándolo antes del bloque de `Seguro de Viajero`. |
| **Seguro de Viajero** | Andy pide que quede como tarjeta separada, pero que **en lugar de tener un interruptor (switch toggle) que se prende y apaga, se coloque un botón que diga "Solicitar"**, el cual debe abrir la página web de AS en la sección de seguros para que puedan contratarlo. | • Tarjeta de seguro de viajero renderizada en perfil. | **En palabras simples:** Quitar el switch de prender/apagar y poner un botón negro que diga "Solicitar" que lleve a la página web para comprar el seguro.<br><br>• *Técnico:* Reemplazar `<Switch checked={...} />` por `<Button onClick={() => window.open('/seguros', '_blank')}>Solicitar</Button>` en `src/app/mobile/perfil/page.tsx`. |
| **Botón Cambiar Contraseña** | Andy indica que al quitar "Cuenta" del menú lateral, se debe agregar en el perfil un botón/tarjeta que diga **"Cambiar contraseña"** colocado **justo encima del botón de "Cerrar sesión"**. | • Pantalla `/mobile/perfil/password` ya construida con validaciones de contraseña actual y nueva. | **En palabras simples:** Poner un botón que diga "Cambiar contraseña" arribita del botón rojo de cerrar sesión.<br><br>• *Técnico:* Añadir una fila de acción interactiva `<div onClick={() => router.push('/mobile/perfil/password')}>` antes del botón de logout en `src/app/mobile/perfil/page.tsx`. |
| **Usuarios Vinculados** | Andy explica que las personas invitadas desde "Crea tu grupo" que aceptan viajar deben sincronizarse y aparecer en el perfil en la sección de **"Usuarios vinculados"**. | • Tabla `linked_travelers` y endpoints creados en base de datos. | **En palabras simples:** Que cuando un amigo acepte la invitación a viajar, aparezca automáticamente en la lista de compañeros de viaje del perfil.<br><br>• *Técnico:* Conectar `GET /api/mobile/linked-travelers` con la aceptación de invitaciones de `group_travel_members` para listar los viajeros vinculados. |

---

### B. 🗺️ Itinerario & Mis Viajes (`/mobile/itinerario`, `/mobile/itinerario/[id]`)
*Fuentes: `Itinerario.mp4`, `Misviajes.mp4`, `Mis viajes icono guardar.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Detección Automática Próximos vs Pasados** | Andy pide que el sistema detecte la fecha actual: si el viaje es a futuro (ej. en un mes), debe salir en "Próximos" indicando cuántos días faltan; si la fecha ya pasó, debe colocarse automáticamente en "Pasados". | • Pestañas de estado y listado de reservas en `/mobile/itinerario`. | **En palabras simples:** Que los viajes que aún no ocurren salgan en la lista de Próximos diciendo cuántos días faltan, y los viejos se pasen solitos a Pasados.<br><br>• *Técnico:* En `src/app/mobile/itinerario/page.tsx`, comparar `new Date(booking.start_date)` contra `new Date()` para clasificar en arrays `upcomingTrips` y `pastTrips` y calcular `Math.ceil((startDate - today) / (1000*60*60*24))` días faltantes. |
| **Quitar Banner "Seleccionando viaje"** | Andy pide **quitar el banner/dropdown superior que dice "Seleccionando viaje: Europa ∨"**. Al dar clic en volver (flecha atrás), debe mandar a la lista general de viajes para elegir otro viaje directamente desde ahí. | • Navegación entre vistas con botón de flecha atrás. | **En palabras simples:** Quitar la barrita desplegable de arriba que dice "Seleccionando viaje" y hacer que la flecha de atrás te regrese a la lista de viajes.<br><br>• *Técnico:* Eliminar el componente `<Select>` de viaje superior en `src/app/mobile/itinerario/[id]/page.tsx` y ajustar `router.back()` hacia `/mobile/itinerario`. |
| **Retorno a la Pestaña Itinerario** | Andy remarcó que cuando estás viendo el detalle de un día del itinerario y le das a "Regresar", **debe mandarte a la pestaña de Itinerario**, NO a la pestaña de Resumen. | • Detalle del día en `/mobile/itinerario/[id]/dia/[dayIndex]`. | **En palabras simples:** Que cuando te salgas de ver el día 3 o día 4 de tu viaje, regreses a la lista de días y no te bote al resumen inicial.<br><br>• *Técnico:* En el botón atrás de `dia/[dayIndex]/page.tsx`, usar `router.push('/mobile/itinerario/' + id + '?tab=itinerario')` en lugar de `router.back()` simple. |
| **Imágenes Reales del Destino por Día** | Andy notó que en el ejemplo del itinerario aparecía la misma foto genérica repetida en todos los días sin importar qué ciudad se visitaba. Pide que **la foto de cada día represente el lugar que se visita** (ej. Londres, París, Roma). | • Renderizado de tarjetas diarias con título, ciudad y actividades. | **En palabras simples:** Que cada día tenga la foto de la ciudad real a la que van (Torre Eiffel en París, Big Ben en Londres) y no la misma foto en todos los días.<br><br>• *Técnico:* En `DestinationContentService.ts` y `itinerario/[id]/page.tsx`, mapear la ciudad de cada día (`day.city`) contra la API de Unsplash/Pexels para almacenar y pintar `day.image_url` específica de cada ciudad. |
| **Cambio a "Recuerda:" en Resumen** | En la pestaña Resumen, Andy pide cambiar el título "Próximos pasos" por **"Recuerda:"** con este listado exacto de tareas:<br>1. *Realizar tus pagos*<br>2. *Subir tus documentos de viaje*<br>3. *Contratar actividades extra*<br>4. *Realizar check-in del vuelo*<br>5. *Disfruta tu viaje, sube una foto y etiquétanos*. | • Checklist interactivo con casillas de verificación. | **En palabras simples:** Cambiar el título "Próximos pasos" por "Recuerda:" y poner los 5 recordatorios exactos que pidió Andy.<br><br>• *Técnico:* Actualizar el título y array de elementos en `src/app/mobile/itinerario/[id]/page.tsx` para renderizar los 5 puntos solicitados con enlaces a sus respectivas pantallas (`/pagos`, `/perfil`, `/tienda`, etc.). |
| **Pestaña Resumen Completa (Servicios)** | Andy indica que en el resumen no solo debe salir el hotel, sino secciones/pestañas para **Vuelos, Alojamiento, Actividades, Seguros y eSIM**, y al tocar cada una debe mostrar los datos reales de la reserva. | • Sección de hotel renderizada en el resumen. | **En palabras simples:** Mostrar no solo el hotel, sino también los boletos de avión, tours extras, seguro y chip de internet que compró el cliente.<br><br>• *Técnico:* Crear acordeones interactivos en la pestaña Resumen para `Vuelos`, `Alojamiento`, `Actividades`, `Seguro` y `eSIM` extrayendo los datos del JSON `special_requests`/`booking_items` de la reserva. |
| **Pestaña Documentos & Requisitos IA** | Andy pide que los documentos personales indiquen si ya fueron subidos y permitan subirlos si faltan. Además, **la IA debe consultar qué documentos son obligatorios según el país destino** (ej. si viaja a Japón, verificar si requiere visa/pasaporte especial) y listar solo los que aplican. Al descargar, debe bajar el PDF real del itinerario, vuelos, seguro y vouchers. | • Pestaña de documentos maquetada con botones de descarga. | **En palabras simples:** Que la inteligencia artificial investigue qué papeles pide el país de destino (ej. Japón) y le diga al cliente cuáles le faltan subir, y que los botones de descarga bajen los PDFs reales.<br><br>• *Técnico:* En `api/itineraries/[id]/requirements`, invocar Gemini/OpenAI con el país destino para retornar el listado de requisitos legales. Conectar los botones de descarga con los endpoints de generación de PDF oficiales. |
| **Ícono Guardar (Modo Offline)** | Andy pide confirmar que el **icono de marcador/guardar en la parte superior derecha funcione para guardar el itinerario y permitir consultarlo sin conexión a internet (offline)**. | • Ícono de marcador visible en la cabecera superior. | **En palabras simples:** Asegurar que al tocar el botón de guardar, todo el itinerario y fotos se queden guardados en el celular para poder verlos en el avión sin internet.<br><br>• *Técnico:* Conectar el botón de guardado con el Service Worker (`sw.ts`) y `IndexedDB` (`localforage`) para precargar los JSONs de la API y assets en la caché local del dispositivo. |
| **Corrección de Traducción en Vivo** | En el documento y vídeo se reporta el error: *"La traducción en tiempo real traduce siempre al inglés a pesar de que no sea el idioma del lugar"*. | • Módulo de traducción y texto a voz (TTS) implementado en el detalle del día. | **En palabras simples:** Arreglar el traductor para que si el viaje es a Francia traduzca al francés, si es a Italia al italiano, y no siempre al inglés.<br><br>• *Técnico:* En `src/app/api/translate/route.ts` y `dia/[dayIndex]/page.tsx`, extraer el código de país/idioma destino (`fr`, `it`, `de`, `ja`, etc.) desde `dayData.practical_info.language` y pasarlo como `targetLang` evitando el fallback a `en`. |

---

### C. 💳 Pagos (`/mobile/pagos`)
*Fuentes: `Pagos.mp4`, `Pagos2.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Botón de Facturación en Pagos Realizados** | Andy pide que en la pestaña de "Realizados" se coloque un **botón de "Facturación" en cada pago**, el cual debe redirigir al usuario directamente a la pantalla de facturación de la web para que genere su CFDI. | • Pestañas "Realizados" y "Por realizar" con diseño de tarjetas limpias. | **En palabras simples:** Poner un botón que diga "Facturar" en los pagos ya hechos para que el cliente pueda sacar su factura del SAT de inmediato.<br><br>• *Técnico:* En la tarjeta de pago realizado de `src/app/mobile/pagos/page.tsx`, añadir `<Button onClick={() => router.push('/facturacion?payment_id=' + pago.id)}>Facturación</Button>`. |
| **Subir Comprobante de Pago en Ventanilla** | En la pestaña de "Por realizar" (pendientes), Andy solicita un **botón para subir comprobante de pago** (foto/PDF) para quienes paguen directamente en ventanilla o transferencia bancaria. | • Botón negro "Pagar ahora" que manda a la pasarela de tarjeta. | **En palabras simples:** Poner un botón para subir la foto del ticket o recibo de transferencia cuando no paguen con tarjeta en línea.<br><br>• *Técnico:* Agregar un botón `<Button variant="outline"><UploadCloud /> Subir Comprobante</Button>` conectado a `ImageUploadInput` (Vercel Blob) que registre la transacción con estado `pending_approval` en `payment_transactions`. |
| **Mensualidades y Fechas Límite** | Andy explica que si el cliente eligió pagar a 6 meses o dio solo el apartado de la reserva, la app debe mostrar **cuántas mensualidades debe, de cuánto es cada pago y la fecha límite para liquidar**, conectado a los datos reales del sistema. | • Desglose de Costo total, Monto pagado y Monto restante en USD/MXN. | **En palabras simples:** Si el cliente compró a meses o dejó apartado, mostrarle qué día vence su próximo pago y cuánto tiene que pagar en cada fecha.<br><br>• *Técnico:* Crear la tabla `booking_payment_schedules` y pintar la tabla de cuotas (número de mensualidad, fecha de vencimiento, monto y estado) en `src/app/mobile/pagos/page.tsx`. |

---

### D. 👥 Crea tu grupo (`/mobile/viajes-grupales`)
*Fuentes: `Crea tu grupo.mp4`, `SisViajesGrupales.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Invitación a un Viaje/Grupo Específico** | Andy señala que en "Crea tu grupo" debe haber una opción para que el usuario **seleccione a qué viaje o grupo en particular va a invitar a sus amigos**, diferenciándolo de la invitación general a la app AS Rewards. | • Generación de código único (ej. `AS-42-YT49`) y botones de compartir en redes sociales. | **En palabras simples:** Que el usuario pueda elegir a cuál de sus viajes quiere invitar a sus amigos (ej. "Invitar al viaje a Europa").<br><br>• *Técnico:* En `src/app/mobile/viajes-grupales/page.tsx`, añadir un selector desplegable con las reservas activas del usuario para que el código y enlace generado sea `https://asoperadora.com/invitacion?booking_id=123&code=...`. |
| **Vincular Amigos con el Perfil** | Andy explica que las personas que acepten la invitación y se unan al grupo deben quedar registradas y aparecer en el apartado de **"Usuarios vinculados"** del Perfil. | • Lista de beneficios por invitar viajeros implementada. | **En palabras simples:** Que los amigos que acepten viajar juntos aparezcan en la lista de acompañantes del perfil.<br><br>• *Técnico:* Al completar el registro con código de grupo en `/api/auth/register`, insertar automáticamente la relación en la tabla `linked_travelers` vinculando al titular y al invitado. |

---

### E. 🛍️ Tienda y Wishlist (`/mobile/tienda`, `Wishlist.mp4`)
*Fuentes: `Wishlist.mp4`, `SisActividades.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Sincronización Wishlist $\leftrightarrow$ Itinerario** | Andy pide confirmar que si un usuario guarda una atracción o souvenir desde el itinerario (ej. Torre de Londres), aparezca en la Wishlist; y si decide quitarlo desde el itinerario o desde la Wishlist, **se elimine en tiempo real en ambos lugares**. | • Botón de corazón interactivo (`WishlistHeart.tsx`) y pantalla dedicada `/mobile/wishlist`. | **En palabras simples:** Que si guardas un monumento o souvenir desde el viaje se guarde en favoritos, y si lo borras de favoritos también se desmarque del viaje.<br><br>• *Técnico:* En `WishlistHeart.tsx` y `useWishlist`, emitir un evento global de ventana (`window.dispatchEvent(new CustomEvent('wishlist-updated'))`) para refrescar el estado en el itinerario y en la vista de Wishlist de inmediato. |
| **Imágenes de Comida Típica** | Andy pide revisar que las imágenes de comida en el itinerario y tienda correspondan fielmente con el platillo típico del lugar. | • Modales de detalle de platillos (`FoodDetailModal.tsx`) con ingredientes y recomendaciones. | **En palabras simples:** Que la foto del platillo sí sea la comida típica de la ciudad visitada.<br><br>• *Técnico:* En `DestinationContentService.ts`, afinar el prompt de generación de IA para que asigne palabras clave precisas de búsqueda de imágenes en Unsplash por cada platillo tradicional. |
| **Regreso al Inicio desde Wishlist** | Andy indica que al salir/dar atrás desde la Wishlist debe llevar a la página principal (`/mobile`) mostrando el mapa y hotel del usuario. | • Vista de mapa con ubicación de hotel y atracciones. | **En palabras simples:** Que la flecha de atrás en la Wishlist te regrese a la pantalla de Inicio de la app.<br><br>• *Técnico:* Configurar `router.push('/mobile')` en el botón `<ChevronLeft>` del header de `src/app/mobile/wishlist/page.tsx`. |

---

### F. 🏆 AS Retos & AS Rewards (`/mobile/rewards`)
*Fuente: `Rewards AS.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Quitar Botón "Ver recompensas" y "Camina y gana"** | Andy pide **quitar el botón de "Ver recompensas"** del banner superior porque no lleva a ningún lado, y **quitar la sección "Camina y gana"** porque ahorita no tiene datos configurados y se ve vacía. | • Pestañas "AS Retos" y "AS Rewards" implementadas. | **En palabras simples:** Quitar el botón de "Ver recompensas" y la sección de "Camina y gana" para que no se vean botones que no hacen nada ni espacios vacíos.<br><br>• *Técnico:* En `src/app/mobile/rewards/page.tsx`, remover el botón `<Button>Ver recompensas</Button>` y comentar/ocultar el bloque JSX de `Camina y Gana`. |
| **Ajuste en Retos (Botón "Ver en mapa")** | Andy señala que en los retos del viaje, los botones "Planear" y "Check-in" marcan los pasos como logrados aunque el usuario no haya ido. Pide **quitar "Planear" y "Check-in" y en su lugar poner un botón "Ver en mapa" en cada lugar** para que trace la ruta desde su hotel. | • Listado de retos por destino (Torre Eiffel, Louvre, etc.) con pasos estimados y modal de mapa. | **En palabras simples:** Quitar los botones de planear y poner en cada lugar un botón de "Ver en mapa" para que el viajero vea cómo caminar hasta allá desde su hotel.<br><br>• *Técnico:* En la lista de retos de `rewards/page.tsx`, eliminar los botones de check-in manual y colocar `<Button onClick={() => openRouteModal(reto)}><MapPin /> Ver en mapa</Button>`. |
| **Nuevo Orden de Secciones en AS Rewards** | Andy solicita reordenar la pantalla de AS Rewards exactamente en esta secuencia:<br>1. *Tu progreso de invitaciones*<br>2. *Invitar más viajeros*<br>3. *Invitados confirmados*<br>4. *Tus beneficios*<br>5. *Beneficios que puedes obtener* | • Barra de progreso (1/30), insignias (Explorador, Embajador, Elite) y lista de invitados confirmados. | **En palabras simples:** Acomodar los bloques de la pantalla en el orden exacto que pidió Andy para que se entienda mejor cómo ganar premios.<br><br>• *Técnico:* Reorganizar el orden de los componentes JSX en la pestaña `rewards` de `src/app/mobile/rewards/page.tsx` siguiendo la secuencia de los 5 puntos indicados. |

---

### G. ❓ ¿Necesitas ayuda? (`/mobile/ayuda`)
*Fuentes: `Necesitas ayuda.mp4`, `Necesitas ayuda 2.mp4`, `WhatsApp Video 2026-08-18 at 9.13.21 PM.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **"Estoy perdido"** | Andy explica que al tocar "Estoy perdido" el sistema debe ubicar el GPS del usuario, **marcar en el mapa su hotel y permitirle generar la ruta para regresar al hotel**, a monumentos, restaurantes o baños públicos cercanos sin errores de mapa. | • Vista de mapa `/mobile/mapa` con geolocalización activa. | **En palabras simples:** Que cuando el usuario presione "Estoy perdido" el mapa le pinte la ruta caminando para volver directo a su hotel o a un baño/restaurante cercano.<br><br>• *Técnico:* En `src/app/mobile/mapa/page.tsx`, precargar las coordenadas del hotel asignado en la reserva y habilitar la API de `DirectionsService` de Google Maps para trazar la ruta paso a paso desde `navigator.geolocation.getCurrentPosition`. |
| **"¿No sé qué actividades hacer?"** | Andy pide **quitar la barra de búsqueda de actividades**. En su lugar, el sistema debe detectar la ciudad donde está el usuario y que la IA sugiera actividades recomendadas con botón para **reservarlas y comprarlas directamente a través de los proveedores**. | • Pantalla `/mobile/actividades-sugeridas` con catálogo y botón "Ver mapa". | **En palabras simples:** Quitar la barrita de escribir y hacer que el sistema le recomiende automáticamente tours según la ciudad donde está, con botón para comprarlos ahí mismo.<br><br>• *Técnico:* En `src/app/mobile/actividades-sugeridas/page.tsx`, remover el `<Input placeholder="Buscar actividad..." />` e inyectar el feed de `ActivityAggregator` filtrado por el destino del viaje activo, conectando el botón de reserva al checkout. |
| **"Perdí mi tour", "Equipaje" y "Call Center"** | Andy confirma textualmente: *"Perdí mi tour o traslado es correcto y los problemas con el equipaje también no hay mayor problema, al igual que si necesitas más ayuda contactar al call center nos pareció que está todo correcto"*. | • Pantallas `/mobile/ayuda/perdi-tour` y `/mobile/ayuda/equipaje` operativas.<br>• Botón de Call Center con apertura a WhatsApp y mensaje precargado. | **En palabras simples:** Esta parte ya fue revisada por Andy y está 100% aprobada.<br><br>• *Técnico:* Mantener el flujo actual en `src/app/mobile/ayuda/page.tsx`, asegurando únicamente que el teléfono de soporte se lea dinámicamente desde `mobile_app_content.help_phone`. |

---

### H. 🍔 Menú de Hamburguesa y Más (`src/app/mobile/page.tsx`, `perfil2.mp4`)
*Fuentes: `perfil2.mp4`, `Wishlist.mp4`, `Pagos2.mp4`*

| Característica / Módulo | 🗣️ Comentarios de Andy (Vídeo / Conversación) | ✅ Lo que ya está Correcto | ⚠️ Lo que hay que Cambiar o Ajustar |
| :--- | :--- | :--- | :--- |
| **Eliminar sección "Cuenta" del Menú Lateral** | Andy menciona que al mover "Cambiar contraseña" adentro del Perfil, **se debe quitar el apartado "Cuenta" del menú de hamburguesa**, dejando únicamente Viajes, Información Legal y Configuración (Perfil / Logout). | • Menú lateral institucional desplegable con avatar y datos del usuario. | **En palabras simples:** Quitar la sección de Cuenta del menú lateral desplegable porque esa opción de contraseña ya se pasó al perfil.<br><br>• *Técnico:* En `src/app/mobile/page.tsx` (Drawer), eliminar el grupo `CUENTA` del array de categorías del menú lateral. |
| **Interferencia de Botones Flotantes (WhatsApp / Chat)** | Andy muestra en el vídeo que los botones flotantes de WhatsApp y Chatbot quedan encimados sobre la barra de navegación inferior y tapan opciones de la pantalla en dispositivos móviles. | • Widgets flotantes de soporte WhatsApp y Chatbot disponibles. | **En palabras simples:** Acomodar los globitos flotantes de WhatsApp y chat para que no tapen los botones ni el menú de abajo.<br><br>• *Técnico:* Ajustar las clases en `src/app/mobile/layout.tsx` a `bottom-20 right-4` y reducir el diámetro a `44px` con un z-index adecuado para que no bloquee el click en elementos del footer ni del menú. |
| **Corrección Tipográfica "Crea tu grupo"** | En vídeos previos se leía *"Creo tu grupo"*. Andy solicitó normalizar el nombre a **"Crea tu grupo"**. | • La ruta y label en el código actual ya está configurado como *"Crea tu grupo"*. | **En palabras simples:** Asegurar que diga "Crea tu grupo" en todas partes.<br><br>• *Técnico:* Verificado en `src/app/mobile/page.tsx` (línea 269) y `viajes-grupales/page.tsx`. |
| **Respetar Color Negro Institucional** | Andy reitera que en toda la aplicación móvil y menús se deben respetar los colores institucionales en **color negro (`#000000` / `#111827`)**. | • Cabeceras y botones principales adaptados a la paleta institucional. | **En palabras simples:** Asegurar que todos los botones y barras sigan viéndose en color negro formal y elegante.<br><br>• *Técnico:* Auditar clases `bg-blue-600` o degradados azules en `/mobile` y estandarizarlas con las clases `bg-gray-900` / `bg-black`. |
